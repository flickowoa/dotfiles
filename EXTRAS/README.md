# EXTRAS

some matching YoRHa css themes i threw together so the apps i use dont break the
style and maintain the UI


- **WhatsappWeb/** — `styleui.css`, drop it into a userstyle manager or whatever like stylus (in my case i have custom whats client))
 
- **Discord** — a nier theme for discord made by `@Accrazed` import `NieR-Light-Source.theme.css`; the `nier/` folder holds the actual stylesheets it pulls in, instead remote imports.
